## 现状审查结论
- README 当前没有“游戏规则”“版本历史”章节；“许可协议”链接指向 LICENSE.txt，但仓库内不存在该文件。
- 仓库内不存在用户所说的 rules.md / game_rules.json；目前只有开发约束类文档位于 [.trae/rules](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/.trae/rules) 与 [.cursor/rules](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/.cursor/rules)。
- “虚假生命”实现点：设置入口在 HP 编辑模态框（输入“虚假生命”并点“刷新”）[index.html:L1385-L1410](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/index.html#L1385-L1410)；扣血优先消耗 tempHp [hp-status.js:L11-L37](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/js/modules/hp-status.js#L11-L37)；setTempHp 为覆盖式写入 [hp-status.js:L39-L44](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/js/modules/hp-status.js#L39-L44)；临时生命随 battle 状态持久化到 localStorage [main.js:L73-L85](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/js/main.js#L73-L85)。

## 需要新增/修改的文档文件
1) 修改 [README.md](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/README.md)
- **“核心特性”新增条目**：在“自动化战斗流程”或“高效操作”下增加“虚假生命（临时生命值）”要点，说明：可设置、会在扣血时优先消耗、为 0 自动隐藏。
- **新增“游戏规则”章节**：新增 `## 游戏规则`，其中包含 `### 虚假生命（临时生命值）`，写清：
  - 获取/设置方式（HP 编辑 → 输入虚假生命 → 刷新）
  - 消耗规则（扣血先扣虚假生命，不足再扣 HP；治疗不影响虚假生命）
  - 覆盖规则（当前实现为“直接设为该值”，不自动取较大值/不叠加）
  - 持久化与范围（随战斗 localStorage 保存；值为 0 不显示）
- **更新“快速开始”**：在“在线使用/本地部署”后补一段“启用/体验虚假生命”的最短步骤（3~4 步 UI 操作）。
- **新增“版本历史”章节**：新增 `## 版本历史`，添加一条记录：引入“虚假生命”支持（并提及显示位置：先攻条卡片与战场总览）。为避免与 package.json 版本号不一致，采用“日期/Unreleased”格式。
- **修复/验证链接**：
  - README 的 LICENSE.txt 链接目前是坏的：要么新增 `LICENSE.txt`（MIT 正文），要么将 README 链接改为仓库实际存在的许可证文件名（若你希望用 LICENSE）。我会按“新增 LICENSE.txt（MIT）”处理，保证链接有效。

2) 新增用户规则文档（满足你提到的 rules.md/game rules 需求）
- 新增根目录 [rules.md](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/rules.md)（或你更偏好 `GAME_RULES.md` 也可，但我会先用 rules.md 以贴合你的命名）。
- 内容结构：
  - `# 游戏规则（实现说明）`
  - `## 虚假生命（临时生命值）`：完整规则与限制条件（基于当前实现行为，而非桌游规则推断）。
  - `## 示例`：用“可复现的 UI 操作示例 + 数字例子（例如：tempHp=8，受伤 10 → tempHp=0、HP-2）”。
  - `## 常见问题（FAQ）`：例如“为什么设为 0 看不到”“为什么治疗不恢复虚假生命”“为什么新加入单位默认没有虚假生命”等。
  - `## 调试/验证`：提供可在浏览器控制台运行的安全片段，例如查看 `localStorage.getItem('dnd-battle-state')` 中的 tempHp（不依赖内部函数暴露）。
- README 的“游戏规则”章节里会链接到该 rules.md。

3)（可选但推荐）同步开发文档一致性
- 在 [.trae/rules/00-project-overview.md](file:///c%3A/Users/ASUS/Desktop/study's/dqy_web/%E9%87%8D%E6%9E%84%E7%89%88D%26D_Tool/.trae/rules/00-project-overview.md) 的“数据模型/状态管理原则”处补一句：battle participant 可能包含 `tempHp`，且随 localStorage 持久化。

## 质量与提交前检查（我会执行的检查项）
- 文档内链接自查：README 内部锚点、外链、以及 LICENSE/rules.md 链接都能打开。
- 术语一致性：统一使用“虚假生命（临时生命值 / Temporary HP）”，并统一字段名为 `tempHp`（只在技术说明处出现）。
- 示例可运行：
  - Bash 示例仅使用已有命令（如 `npx http-server -p 8080 -o`）。
  - 控制台示例只用标准 Web API（localStorage/JSON.parse）。
- 拼写与语法：对新增段落做一次通读修正。
- 团队评审：我会在 README/rules.md 顶部给出“评审清单”小节，方便你发给团队成员做 review（我无法直接获取团队成员意见，但会把评审点写清）。

## 实施范围（将改动/新增的文件）
- 修改：README.md
- 新增：rules.md
- 新增或调整：LICENSE.txt（用于修复 README 许可链接）
-（可选）修改：.trae/rules/00-project-overview.md
